---
apiVersion: apps/v1
kind: Deployment
metadata:
  {{ if eq .KUBE_NAMESPACE .BRANCH_ENV }}
  name: redis-{{ .DRONE_SOURCE_BRANCH }}
  {{ else }}
  name: redis
  {{ end }}
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      {{ if eq .KUBE_NAMESPACE .BRANCH_ENV }}
      app: redis-{{ .DRONE_SOURCE_BRANCH }}
      {{ else }}
      app: redis
      {{ end }}
  template:
    metadata:
      labels:
        {{ if eq .KUBE_NAMESPACE .BRANCH_ENV }}
        name: redis-{{ .DRONE_SOURCE_BRANCH }}
        service: redis-{{ .DRONE_SOURCE_BRANCH }}
        app: redis-{{ .DRONE_SOURCE_BRANCH }}
        {{ else }}
        name: redis
        service: redis
        app: redis
        {{ end }}
    
    spec:
      securityContext:
        fsGroup: 994
        fsGroupChangePolicy: OnRootMismatch
      containers:
        - name: redis
          # redis:latest
          image: quay.io/ukhomeofficedigital/hof-docker-redis@sha256:921422bacbcc6af3615dd031499e285fbf955770f491d241956dd11ce4a67228
          env:
            # Enable shell command tracing in run.sh for troubleshooting startup issues. Keep false by default to avoid noisy pod logs.   
            - name: DEBUG
              value: "false"
          ports:
            - containerPort: 6379
          startupProbe:
            tcpSocket:
              port: 6379
            periodSeconds: 5
            failureThreshold: 24
          readinessProbe:
            tcpSocket:
              port: 6379
            initialDelaySeconds: 2
            periodSeconds: 5
            failureThreshold: 3
          livenessProbe:
            tcpSocket:
              port: 6379
            initialDelaySeconds: 15
            periodSeconds: 10
            failureThreshold: 3
          volumeMounts:
            - mountPath: /var/lib/redis
              name: data
          securityContext:
            runAsNonRoot: true
          resources:
            requests:
              cpu: "20m"
              memory: "100Mi"
            limits:
              cpu: "100m"
              memory: "200Mi"
      volumes:
        - name: data
          {{ if eq .REDIS_PERSISTENCE_ENABLED "true" }}
          persistentVolumeClaim:
            {{ if .REDIS_PERSISTENCE_EXISTING_CLAIM }}
            claimName: {{ .REDIS_PERSISTENCE_EXISTING_CLAIM }}
            {{ else }}
            claimName: redis-data
            {{ end }}
          {{ else }}
          emptyDir: {}
          {{ end }}
